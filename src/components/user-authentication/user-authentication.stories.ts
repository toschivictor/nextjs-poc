import type { Meta } from '@storybook/react';

import { UserAuthentication } from './user-authentication';

const meta: Meta<typeof UserAuthentication> = {
	title: 'Components/UserAuthentication',
	component: UserAuthentication,
};

export default meta;
